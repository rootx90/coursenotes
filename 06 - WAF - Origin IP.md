---
title: WAF - Origin IP
date: 2026-04-26
tags:
  - waf
  - origin-ip
  - bug-bounty
  - pentest
---

# WAF - Origin IP

[[Index|Back to Index]]

> [!summary] الفكرة الأساسية
> الـ WAF أو Web Application Firewall بيكون طبقة حماية بين المستخدمين والسيرفر الأصلي. الهدف إن كل الترافيك يدخل على الـ WAF الأول، وبعدها الـ WAF يوصله للـ origin server.

لو حد عرف الـ origin IP ممكن يتخطى الـ WAF ويوصل للسيرفر مباشرة، وده يقلل أو يلغي فائدة الحماية.

## كيف يحمي الـ WAF الـ Origin IP؟

- يخفي عنوان السيرفر الحقيقي خلف عنوان أو شبكة الـ WAF.
- يفحص الطلبات قبل ما توصل للتطبيق.
- يمنع هجمات شائعة مثل SQL Injection و XSS وبعض أنواع DDoS.
- يسمح بوضع قواعد تمنع الطلبات المشبوهة.
- يقلل ظهور السيرفر الأصلي على الإنترنت مباشرة.

## كيف نحمي الـ Origin IP بشكل صحيح؟

> [!important] أهم قاعدة
> وجود WAF وحده لا يكفي. لازم السيرفر الأصلي لا يقبل Web traffic إلا من الـ WAF فقط.

- السماح بالوصول للسيرفر فقط من IP ranges الخاصة بالـ WAF.
- إغلاق الوصول المباشر من الإنترنت على منافذ الويب مثل 80 و 443.
- استخدام firewall rules على السيرفر أو cloud security group.
- عدم نشر الـ origin IP في DNS records مباشرة.
- حذف أو حماية أي records قد تكشف الـ IP مثل old A records أو subdomains.
- استخدام SSL/TLS بين الـ WAF والـ origin.

## كيف يمكن الوصول إلى الـ Origin IP؟

طرق شائعة قد تكشف الـ origin IP:

- DNS history أو سجلات قديمة قبل استخدام الـ WAF.
- Subdomains غير محمية بالـ WAF وتشير لنفس السيرفر.
- رسائل البريد أو headers تكشف IP داخلي أو خارجي.
- شهادات SSL قديمة أو سجلات Certificate Transparency.
- أخطاء في إعدادات السيرفر أو response headers.
- خدمات أخرى مفتوحة على نفس السيرفر مثل SSH أو FTP أو mail.

## الخلاصة

الـ WAF يحمي التطبيق عندما يكون هو الطريق الوحيد للوصول للسيرفر. لو الـ origin IP مكشوف أو السيرفر يقبل اتصال مباشر من الإنترنت، يمكن تجاوز طبقة الحماية.
