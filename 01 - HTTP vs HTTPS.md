---
title: HTTP vs HTTPS
date: 2026-04-26
tags:
  - http
  - https
  - tls
  - encryption
---

# HTTP و HTTPS


## ما هو HTTP؟

HTTP اختصار لـ HyperText Transfer Protocol.
هو البروتوكول المستخدم لنقل البيانات بين المتصفح والسيرفر.

في HTTP البيانات تنتقل بدون تشفير، لذلك أي طرف بين المستخدم والسيرفر قد يستطيع قراءة البيانات إذا كان عنده القدرة على مراقبة الاتصال.

### أمثلة على بيانات ممكن تنكشف في HTTP

- اسم المستخدم وكلمة المرور.
- Cookies.
- محتوى الصفحات.
- البيانات المرسلة في forms.

## ما هو HTTPS؟

HTTPS هو HTTP لكن مع طبقة حماية اسمها TLS.
TLS اختصار لـ Transport Layer Security.

HTTPS يوفر:

- Encryption: تشفير البيانات.
- Authentication: التأكد من هوية الموقع.
- Integrity: التأكد أن البيانات لم يتم تعديلها أثناء النقل.

## الفرق بين HTTP و HTTPS

| النقطة | HTTP | HTTPS |
|---|---|---|
| التشفير | لا يوجد | يوجد TLS |
| الأمان | ضعيف | أقوى |
| المنفذ الافتراضي | 80 | 443 |
| الشهادة | لا يحتاج | يحتاج SSL/TLS certificate |
| حماية البيانات | لا يحميها | يحميها أثناء النقل |

## طرق التشفير في HTTPS

HTTPS يستخدم نوعين مهمين من التشفير:

- Asymmetric Encryption: تشفير غير متماثل، يستخدم public key و private key.
- Symmetric Encryption: تشفير متماثل، يستخدم session key واحد بعد إنشاء الاتصال الآمن.

## خطوات عمل HTTPS / TLS Handshake

### 1. Client Hello

المتصفح يبدأ الاتصال ويرسل للسيرفر:

- نسخة TLS المدعومة.
- قائمة خوارزميات التشفير المدعومة.
- رقم عشوائي اسمه Client Random.

### 2. Server Hello

السيرفر يرد ويرسل:

- نسخة TLS التي سيتم استخدامها.
- خوارزمية التشفير المختارة.
- رقم عشوائي اسمه Server Random.
- شهادة SSL/TLS الخاصة بالموقع.

### 3. التحقق من الشهادة

المتصفح يفحص الشهادة ويتأكد من:

- الشهادة صادرة من Certificate Authority موثوقة.
- اسم الدومين في الشهادة يطابق الموقع.
- الشهادة لم تنته صلاحيتها.
- الشهادة لم يتم إلغاؤها.

> عند فشل التحقق
> إذا فشل التحقق من الشهادة، المتصفح يظهر تحذير أمان.

### 4. تبادل المفاتيح Key Exchange

بعد التحقق من الشهادة، يتم إنشاء مفتاح مشترك بين المتصفح والسيرفر.

في الإصدارات الحديثة غالبا يتم استخدام ECDHE.
هذا يسمح بإنشاء session key بدون إرسال المفتاح نفسه بشكل مباشر عبر الشبكة.

### 5. إنشاء Session Key

المتصفح والسيرفر يستخدمان القيم العشوائية ونتيجة تبادل المفاتيح لإنشاء session key.

هذا المفتاح يستخدم لتشفير البيانات أثناء الجلسة.

### 6. استخدام Symmetric Encryption

بعد إنشاء session key، يتم استخدام التشفير المتماثل لأنه أسرع من التشفير غير المتماثل.

كل البيانات بين المتصفح والسيرفر تكون مشفرة باستخدام هذا المفتاح.

### 7. التحقق من سلامة البيانات

TLS يتأكد أن البيانات لم يتم تعديلها أثناء النقل باستخدام آليات integrity مثل MAC أو AEAD حسب نوع الخوارزمية.

لو تم تعديل البيانات أثناء النقل، الاتصال يعتبر غير صالح.

## لماذا لا يستخدم HTTPS التشفير غير المتماثل طوال الوقت؟

لأن التشفير غير المتماثل أبطأ ومكلف أكثر.
لذلك يتم استخدامه فقط في البداية للتحقق وتبادل المفاتيح، وبعدها يستخدم HTTPS التشفير المتماثل للسرعة.

## الخلاصة

HTTP ينقل البيانات بدون تشفير.
HTTPS يحمي الاتصال باستخدام TLS، ويبدأ بتبادل آمن للمفاتيح ثم يستخدم session key لتشفير البيانات بسرعة أثناء الجلسة.
